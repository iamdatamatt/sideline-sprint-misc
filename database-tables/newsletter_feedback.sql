CREATE TABLE newsletter_feedback (
    email varchar(256),
    referral_id varchar(256),
    campaign varchar(256),
    liked_newsletter varchar(256),
    ip_address varchar(256),
    timestamp_utc timestamp
);